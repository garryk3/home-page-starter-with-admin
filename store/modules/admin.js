// import * as types from '../mutation-types'

const state = {
  categories: []
}

const getters = {
  categories: (state) => state.categories
}

const actions = {}

const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}
