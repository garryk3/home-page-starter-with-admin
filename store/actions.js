// import * as types from './mutation-types'// import {http} from '~/plugins/axios'

// export const getCategories = ({commit}, categories) => {
//   if (Array.isArray(categories) && categories.length) {
//     commit(types.GET_CATEGORIES, categories)
//   }
// }
